This file is just being used to test the build triggers in the CI server.

Note that this is on a feature branch ('feature/ci-integration') that won't be merged into master - or at least the changes used for testing build triggers will not.
